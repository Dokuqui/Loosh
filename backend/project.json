{
  "name": "backend",
  "root": "backend",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd backend/SmartHomeApi/ && dotnet build"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd backend/SmartHomeApi/ && dotnet run"
      }
    }
  }
}
